#include <fstream>
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    string input;
    ifstream file;

    file.open("../input");

    while (getline(file, input)) {
    
    }

    return 0;
}